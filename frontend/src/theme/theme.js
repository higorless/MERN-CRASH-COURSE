import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    primary: {
      100: "#E5FCF1",
      500: "#00A3C4",
      900: "#1A365D",
    },
  },
});

export default theme;
